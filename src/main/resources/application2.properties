#############################################################
# 服务配置
#############################################################
server.port = 8000
# spring boot 默认的context-path 是/
#server.context-path=/xtn/api

# server.session-timeout= 3000
# server.servlet-path= # servlet 默认路径是 /
# server.tomcat.access-log-pattern
# server.tomcat.access-log-enabled=false # 
# server.tomcat.protocol-header=x-forwarded-proto # ssl forward headers
# server.tomcat.remote-ip-header=x-forwarded-for
# server.tomcat.basedir = /tmp # base dir (usually not needed ,defaults to tmp)
# server.tomcat.max-threads=0   # number of threads in protocol handler
# server.tomcat.uri-encoding = utf-8 # character encoding to use for url decoding



#############################################################
# thymeleaf 配置信息
#############################################################
spring.thymeleaf.prefix=classpath:/templates/
spring.thymeleaf.suffix=.html
spring.thymeleaf.mode=HTML5
spring.thymeleaf.encoding=UTF-8
spring.thymeleaf.cache=false         # 关闭缓存







#############################################################
# 数据库配置信息
#############################################################
spring.datasource.url=jdbc:mysql://127.0.0.1:3306/nodejs?useSSL=false
spring.datasource.username=liaohb
spring.datasource.password=xiaotuni
spring.datasource.driver-class-name=com.mysql.jdbc.Driver
spring.datasource.max-idle=10
spring.datasource.max-wait=10000
spring.datasource.min-idle=5
spring.datasource.initial-size=5


# Specify the DBMS
spring.jpa.database = MYSQL
# Show or not log for each sql query
spring.jpa.show-sql = true
# Hibernate ddl auto (create, create-drop, update)
spring.jpa.hibernate.ddl-auto = update
# Naming strategy
spring.jpa.hibernate.naming-strategy = org.hibernate.cfg.ImprovedNamingStrategy

# stripped before adding them to the entity manager)
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQL5Dialect
